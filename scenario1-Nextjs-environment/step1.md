# Step 1: 環境の準備

まず、Next.js のプロジェクトが正しくクローンされているか確認しましょう。

```bash
cd /root/next-env-demo
ls
```

次のファイルが見えればOKです：

```
.env.local
package.json
src/
```

次に依存パッケージをインストールします。

```bash
npm install
```
